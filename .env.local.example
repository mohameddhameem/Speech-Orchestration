# Local Development Environment Configuration
# Copy this file to .env and uncomment the lines you need

# ============================================================================
# ENVIRONMENT MODE
# ============================================================================
# Set ENVIRONMENT to LOCAL to use RabbitMQ, local storage, and HuggingFace models
# Set ENVIRONMENT to AZURE to use Azure services with DefaultAzureCredential
ENVIRONMENT=LOCAL

# ============================================================================
# LOCAL MODE CONFIGURATION
# ============================================================================

# RabbitMQ Configuration (used when ENVIRONMENT=LOCAL)
RABBITMQ_URL=amqp://guest:guest@localhost:5672/

# Local Storage Configuration (used when ENVIRONMENT=LOCAL)
LOCAL_STORAGE_PATH=/tmp/speech-flow-storage

# HuggingFace Model Configuration (used when ENVIRONMENT=LOCAL)
HF_SUMMARIZATION_MODEL=facebook/bart-large-cnn
HF_TRANSLATION_MODEL=Helsinki-NLP/opus-mt-{src}-{tgt}

# ============================================================================
# AZURE MODE CONFIGURATION (with DefaultAzureCredential)
# ============================================================================

# Azure Storage Configuration (Production - uses DefaultAzureCredential)
# AZURE_STORAGE_ACCOUNT_NAME=mystorageaccount
# AZURE_STORAGE_ACCOUNT_URL=https://mystorageaccount.blob.core.windows.net

# Azure Storage Configuration (Azurite - uses connection string)
# Uncomment for local Azurite testing
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;...

# Azure Service Bus Configuration (Production - uses DefaultAzureCredential)
# SERVICEBUS_NAMESPACE=myservicebus
# SERVICEBUS_FQDN=myservicebus.servicebus.windows.net

# Azure Service Bus Configuration (Legacy - uses connection string)
# Uncomment for legacy authentication or local testing
# SERVICEBUS_CONNECTION_STRING=Endpoint=sb://myservicebus.servicebus.windows.net/;...

# Azure OpenAI Configuration (Production - uses DefaultAzureCredential)
# AZURE_OPENAI_ENDPOINT=https://myresource.openai.azure.com/
# AZURE_OPENAI_DEPLOYMENT=gpt-4

# Azure OpenAI Configuration (Legacy - uses API key)
# Uncomment for API key authentication
# AZURE_OPENAI_KEY=your-api-key-here
# AZURE_OPENAI_INPUT_COST=0.01
# AZURE_OPENAI_OUTPUT_COST=0.03

# ============================================================================
# COMMON CONFIGURATION
# ============================================================================

# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/speechflow

# Queue Names (same for both local and Azure)
ROUTER_QUEUE_NAME=job-events
LID_QUEUE_NAME=lid-jobs
WHISPER_QUEUE_NAME=whisper-jobs
AZURE_AI_QUEUE_NAME=azure-ai-jobs

# Blob Container Names
BLOB_CONTAINER_NAME=raw-audio
BLOB_CONTAINER_RESULTS=results

# Model Cache Directory
MODEL_CACHE_DIR=/models

# ============================================================================
# AUTHENTICATION NOTES
# ============================================================================
#
# DefaultAzureCredential supports multiple authentication methods (in order):
# 1. Environment Variables (AZURE_CLIENT_ID, AZURE_CLIENT_SECRET, AZURE_TENANT_ID)
# 2. Managed Identity (UAMI or SAMI)
# 3. Azure CLI
# 4. Azure PowerShell
# 5. Interactive browser
#
# For production deployments:
# - Use Managed Identity (SAMI or UAMI) - most secure
# - Or set Client Secret via environment variables:
#   AZURE_CLIENT_ID=your-client-id
#   AZURE_CLIENT_SECRET=your-client-secret
#   AZURE_TENANT_ID=your-tenant-id
#
# ============================================================================
